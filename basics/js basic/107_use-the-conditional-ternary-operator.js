function checkEqual(a, b) {
  return a == b ? "Equal" : "Not Equal";
  // Ternary Operators can be chained similar to if..else if....else 
  // return a < b ? "b is greater" : a > b ? "a is greater" : a == b ? "is equal": "test";        
}

console.log(checkEqual(1, 2));
